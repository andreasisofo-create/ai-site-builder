# FastAPI
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.9

# Config
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Validation
email-validator>=2.0.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.17

# HTTP Client (OAuth, AI service)
httpx==0.27.2

# Rate Limiting
slowapi==0.1.9

# Email (Resend)
resend>=2.5.0

# Payments (Revolut - uses httpx, no SDK needed)

# Note: chromadb removed — replaced with lightweight in-memory keyword search
# to stay within Render free tier 512MB memory limit.
# pgvector removed — V2 pipeline works when installed locally but not needed
# on production (protected by try/except in models/__init__.py)

# Image Generation (Flux via fal.ai)
fal-client>=0.5.0
